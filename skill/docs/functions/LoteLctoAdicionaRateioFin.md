# LoteLctoAdicionaRateioFin

## Assinatura

```lspt
Funcao LoteLctoAdicionaRateioFin(Numero pCtaRed, Alfa pCodCcu, Numero pPerRat, Numero pVlrRat,Numero pCtaFin);
```

## Código
N/A

## Descrição

Esta função adiciona os rateios em uma estrutura em memória para serem gerados posteriormente.

## Parâmetros

- **pCtaRed** (`Numero`) - Entrada: Variável que recebe o código da conta reduzida.
- **pCodCcu** (`Alfa`) - Entrada: Variável que recebe o código do centro de custos.
- **pPerRat** (`Numero`) - Entrada: Variável que recebe o percentual do rateio.
- **pVlrRat** (`Numero`) - Entrada: Variável que recebe o valor do rateio.
- **pCtaFin** (`Numero`) - Entrada: Variável que recebe o código da conta financeira.

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
LoteLctoAdicionaRateioFin();
```

## Fonte

[Documentação oficial Senior](https://documentacao.senior.com.br/gestaoempresarialerp/5.10.4/regra_funcoes/lotelctoadicionarateiofin.htm)

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
