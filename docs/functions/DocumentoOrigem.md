# DocumentoOrigem

## Assinatura

```lspt
Funcao DocumentoOrigem(Numero pCodEmp, Numero pNumLct, Alfa pOriLct, Alfa End pChaveOrigem);
```

## Código
N/A

## Descrição

Retornar a chave do documento origem conforme a lançamento origem.

## Parâmetros

- **pCodEmp** (`Numero`) - Entrada: Variável que indica o código da empresa.
- **pNumLct** (`Numero`) - Entrada: Variável que indica o número do lançamento.
- **pOriLct** (`Alfa`) - Entrada: Variável que indica a origem do lançamento.
- **pChaveOrigem** (`Alfa End`) - Saída: Variável que retorna a chave do documento origem.

## Valores de Retorno

- pChaveOrigem: Variável alfanumérica que retorna a chave do documento
- origem;

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
DocumentoOrigem();
```

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
