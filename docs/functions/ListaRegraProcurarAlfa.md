# ListaRegraProcurarAlfa

## Assinatura

```lspt
Funcao ListaRegraProcurarAlfa(Numero aLista, Alfa aColuna, Alfa aValor, Alfa End aExisteRegistro);
```

## Código
N/A

## Descrição

Função responsável para pesquisar valor com tipo alfa na lista.

## Parâmetros

- **aLista** (`Numero`) - Entrada: variável numérica que recebe o endereço da lista
- **aColuna** (`Alfa`) - Entrada: variável alfa que recebe o nome da coluna a qual deseja obter o valor
- **aValor** (`Alfa`) - Entrada: variável alfa que recebe o valor da coluna a ser procurado
- **aExisteRegistro** (`Alfa End`) - Saída

## Valores de Retorno

- aExisteRegistro: Indica se existe valor na lista. 'S' para existe e 'N' para não existe.

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
ListaRegraProcurarAlfa();
```

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
