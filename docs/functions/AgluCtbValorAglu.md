# AgluCtbValorAglu

## Assinatura

```lspt
Funcao AgluCtbValorAglu(Numero pCodAgl, Numero pDatIni, Numero pDatFim, Numero End Retorno);
```

## Código
N/A

## Descrição

Buscar o valor do aglutinador contábil no período informado.

## Parâmetros

- **pCodAgl** (`Numero`) - Entrada: Variável que recebe o código do aglutinador.
- **pDatIni** (`Numero`) - Entrada: Variável que recebe a data de inicial(opcional).
- **pDatFim** (`Numero`) - Entrada: Variável que recebe a data final(opcional).
- **Retorno** (`Numero End`) - Saída: Variável que indica o valor do registro posicionado.

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
AgluCtbValorAglu();
```

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
