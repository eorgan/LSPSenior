# ListaRegraAlterarLinhaData

## Assinatura

```lspt
Funcao ListaRegraAlterarLinhaData(Numero aLista, Alfa aColuna, Data aValor, Alfa End aExecutou);
```

## Código
N/A

## Descrição

Função responsável por alerar valor de uma coluna em uma linha da lista.

## Parâmetros

- **aLista** (`Numero`) - Entrada: variável numérica que recebe o endereço da lista
- **aColuna** (`Alfa`) - Entrada: variável alfa que recebe o nome da coluna
- **aValor** (`Data`) - Entrada: variável numérica que recebe o novo valor da coluna a ser alterado
- **aExecutou** (`Alfa End`) - Saída

## Valores de Retorno

- aExecutou: Indica se foi alterado valor. "S" para alterado ou "N" para não alterado.

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
ListaRegraAlterarLinhaData();
```

## Fonte

[Documentação oficial Senior](https://documentacao.senior.com.br/gestaoempresarialerp/5.10.4/regra_funcoes/listaregraalterarlinhadata.htm)

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
