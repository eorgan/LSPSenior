# ListaRegraSalvarLista

## Assinatura

```lspt
Funcao ListaRegraSalvarLista(Numero aLista, Alfa aCaminho, Alfa aNomeArquivo, Alfa aFormato);
```

## Código
N/A

## Descrição

Função responsável por salvar a lista em arquivo, Arquivo pode ser salvo em dois formatos ".txt" ou ".csv".

## Parâmetros

- **aLista** (`Numero`) - Entrada: variável numérica que recebe o endereço da lista
- **aCaminho** (`Alfa`) - Entrada: variável alfa que recebe o caminho onde a lista será salva
- **aNomeArquivo** (`Alfa`) - Entrada: variável alfa que recebe o nome do arquivo
- **aFormato** (`Alfa`) - Entrada: variável alfa que recebe formato que o arquivo será salvo ("T" formato .txt ou "C" formato .csv).

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
ListaRegraSalvarLista();
```

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
