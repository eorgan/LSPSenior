# MMCalculaMultaTituloCR

## Assinatura

```lspt
Funcao MMCalculaMultaTituloCR(Numero pDatBas, Numero pCepCli);
```

## Código
N/A

## Descrição

Esta função tem por objetivo calcular o valor da multa do contas a receber, de acordo com uma data base e CEP do cliente.

## Parâmetros

- **pDatBas** (`Numero`) - Entrada: Campo, Valor ou Variável numérica data base de cálculo para o desconto do contas a receber
- **pCepCli** (`Numero`) - Entrada: CEP do cliente para a busca de feriados

## Valores de Retorno

- o valor da multa do contas a receber.

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
MMCalculaMultaTituloCR();
```

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
