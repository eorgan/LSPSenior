# ListaRegraAlterarLinhaNumero

## Assinatura

```lspt
Funcao ListaRegraAlterarLinhaNumero(Numero aLista, Alfa aColuna, Numero aValor, Alfa End aExecutou);
```

## Código
N/A

## Descrição

Função responsável por alterar valor de uma coluna em uma linha da lista.

## Parâmetros

- **aLista** (`Numero`) - Entrada: variável numérica que recebe o endereço da lista
- **aColuna** (`Alfa`) - Entrada: variável alfa que recebe o nome da coluna
- **aValor** (`Numero`) - Entrada: variável numérica que recebe o novo valor da coluna a ser alterado
- **aExecutou** (`Alfa End`) - Saída

## Valores de Retorno

- aExecutou: Indica se foi alterado valor. "S" para alterado ou "N" para não alterado.

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
ListaRegraAlterarLinhaNumero();
```

## Fonte

[Documentação oficial Senior](https://documentacao.senior.com.br/gestaoempresarialerp/5.10.4/regra_funcoes/listaregraalterarlinhanumero.htm)

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
