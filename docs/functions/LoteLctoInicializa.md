# LoteLctoInicializa

## Assinatura

```lspt
Funcao LoteLctoInicializa(Numero pCodEmp, Numero pCodFil, Numero pDatLot, Alfa pViaExe);
```

## Código
N/A

## Descrição

Esta função inicializa as variáveis utilizadas na importação de lançamentos contábeis.

## Parâmetros

- **pCodEmp** (`Numero`) - Entrada: Variável que recebe o código da empresa.
- **pCodFil** (`Numero`) - Entrada: Variável que recebe o código da filial (filial que será gravada no lote).
- **pDatLot** (`Numero`) - Entrada: Variável que recebe a data do lote.
- **pViaExe** (`Alfa`) - Entrada: Variável que recebe a via de execução da função (Ex:'IMPORT' quando a função é chamada pela importação).

## Exemplo de Uso

```lspt
@-- Adicione exemplo de uso aqui --@
LoteLctoInicializa();
```

> Gerado automaticamente a partir da documentação oficial da LSP por `generate-functions.js`.
> Arquivos com esta nota são regenerados a cada execução; remova-a para editar manualmente
> sem ser sobrescrito.
